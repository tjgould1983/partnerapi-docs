post:
  tags:
  - order-api
  summary: "Create order (v1)"
  requestBody:
    required: true
    content:
      application/json:
        schema:
          "$ref": "#/definitions/order-request"
  responses:
    '201':
      description: Created